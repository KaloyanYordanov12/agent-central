fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.25

# Secretary agent — background activity indexer (Step 2)
chromadb>=0.4.22
sentence-transformers>=2.7.0

# Secretary agent — Ask endpoint / RAG (Step 3)
anthropic>=0.40.0

# Job Scout agent — RSS/JSON job-source discovery (commit 1)
feedparser>=6.0
